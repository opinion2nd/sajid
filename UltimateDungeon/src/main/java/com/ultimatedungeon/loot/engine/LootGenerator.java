package com.ultimatedungeon.loot.engine;

import com.ultimatedungeon.core.PluginLogger;
import org.jetbrains.annotations.NotNull;

/** LootGenerator — loot generation engine. Implemented in Milestone 6. */
public final class LootGenerator {
    private final PluginLogger logger;
    public LootGenerator(@NotNull final PluginLogger logger) { this.logger = logger; }
}
