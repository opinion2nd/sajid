package com.ultimatedungeon.config;

/** Defines the current configuration schema version. Increment on breaking changes. */
public final class ConfigVersion {
    public static final int CURRENT = 1;
    private ConfigVersion() {}
}
