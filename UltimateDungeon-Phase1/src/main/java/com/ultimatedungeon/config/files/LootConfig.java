package com.ultimatedungeon.config.files;

/** Configuration wrapper for LootConfig.yml. Populated by {@link com.ultimatedungeon.config.ConfigManager}. */
public final class LootConfig {
    // Values populated during Phase 1 implementation.
    private LootConfig() {}
}
