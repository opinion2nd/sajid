package com.ultimatedungeon.config.files;

/** Configuration wrapper for DifficultyConfig.yml. Populated by {@link com.ultimatedungeon.config.ConfigManager}. */
public final class DifficultyConfig {
    // Values populated during Phase 1 implementation.
    private DifficultyConfig() {}
}
