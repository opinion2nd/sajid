package com.ultimatedungeon.config.files;

/** Configuration wrapper for BossesConfig.yml. Populated by {@link com.ultimatedungeon.config.ConfigManager}. */
public final class BossesConfig {
    // Values populated during Phase 1 implementation.
    private BossesConfig() {}
}
