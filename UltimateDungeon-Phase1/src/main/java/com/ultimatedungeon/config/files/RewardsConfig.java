package com.ultimatedungeon.config.files;

/** Configuration wrapper for RewardsConfig.yml. Populated by {@link com.ultimatedungeon.config.ConfigManager}. */
public final class RewardsConfig {
    // Values populated during Phase 1 implementation.
    private RewardsConfig() {}
}
