package com.ultimatedungeon.config.files;

/** Configuration wrapper for MessagesConfig.yml. Populated by {@link com.ultimatedungeon.config.ConfigManager}. */
public final class MessagesConfig {
    // Values populated during Phase 1 implementation.
    private MessagesConfig() {}
}
