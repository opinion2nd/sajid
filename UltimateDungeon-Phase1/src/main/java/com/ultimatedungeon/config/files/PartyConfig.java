package com.ultimatedungeon.config.files;

/** Configuration wrapper for PartyConfig.yml. Populated by {@link com.ultimatedungeon.config.ConfigManager}. */
public final class PartyConfig {
    // Values populated during Phase 1 implementation.
    private PartyConfig() {}
}
