package com.ultimatedungeon.loot.engine;

import com.ultimatedungeon.core.PluginLogger;
import org.jetbrains.annotations.NotNull;

/** RarityRoller — loot generation engine. Implemented in Milestone 6. */
public final class RarityRoller {
    private final PluginLogger logger;
    public RarityRoller(@NotNull final PluginLogger logger) { this.logger = logger; }
}
