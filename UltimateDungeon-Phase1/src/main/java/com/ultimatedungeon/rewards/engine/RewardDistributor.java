package com.ultimatedungeon.rewards.engine;

import com.ultimatedungeon.core.PluginLogger;
import org.jetbrains.annotations.NotNull;

/** RewardDistributor — reward distribution engine. Implemented in Milestone 6. */
public final class RewardDistributor {
    private final PluginLogger logger;
    public RewardDistributor(@NotNull final PluginLogger logger) { this.logger = logger; }
}
