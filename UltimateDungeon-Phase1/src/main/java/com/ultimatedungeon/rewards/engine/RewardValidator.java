package com.ultimatedungeon.rewards.engine;

import com.ultimatedungeon.core.PluginLogger;
import org.jetbrains.annotations.NotNull;

/** RewardValidator — reward distribution engine. Implemented in Milestone 6. */
public final class RewardValidator {
    private final PluginLogger logger;
    public RewardValidator(@NotNull final PluginLogger logger) { this.logger = logger; }
}
