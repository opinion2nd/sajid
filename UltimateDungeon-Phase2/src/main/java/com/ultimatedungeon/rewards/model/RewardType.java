package com.ultimatedungeon.rewards.model;

/** All reward types the plugin can deliver to players. */
public enum RewardType {
    MONEY, EXPERIENCE, ITEM, KEY, TOKEN, CUSTOM_COMMAND, PERMISSION, COSMETIC
}
