package com.ultimatedungeon.rewards.engine;

import com.ultimatedungeon.core.PluginLogger;
import org.jetbrains.annotations.NotNull;

/** RewardRoomService — reward distribution engine. Implemented in Milestone 6. */
public final class RewardRoomService {
    private final PluginLogger logger;
    public RewardRoomService(@NotNull final PluginLogger logger) { this.logger = logger; }
}
