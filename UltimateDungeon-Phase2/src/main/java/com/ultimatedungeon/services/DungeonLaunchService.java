package com.ultimatedungeon.services;

import com.ultimatedungeon.core.PluginLogger;
import org.jetbrains.annotations.NotNull;

/** DungeonLaunchService — stateless service. Implemented in the relevant milestone. */
public final class DungeonLaunchService {
    private final PluginLogger logger;
    public DungeonLaunchService(@NotNull final PluginLogger logger) { this.logger = logger; }
}
